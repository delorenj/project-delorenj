---
topic: Career Ascent — Jarad DeLorenzo cinematic scroll-driven career-world portfolio; unify PRD + architecture spine into the machine contract
updated: 2026-07-16T10:02
---

- (direction) Invoked by /loop (autonomous, Fast path) as phase 1 of spec->epics pipeline. Mode=express/headless-style: inputs pre-supplied (PRD + finalized architecture spine), derive SPEC.md autonomously, surface open questions, do NOT run interactive Story Breakdown (phase 2 = bmad-create-epics-and-stories owns epics/stories). Input is structured+pre-sorted => trust authored separation; lift kernel-fitting to SPEC.md, keep PRD + spine as ADOPTED companions (downstream reads both directly). Slug=project-delorenj inherited from PRD folder.
- (capability) CAP-1 Pilot the career by scrolling. intent: visitor scrolls to move continuously through the career; scroll deterministically maps to a monotonic Career Sequence position driving camera altitude + a derived year label. success: at rest the reported Era/label matches the scroll-derived position via the Sequence-to-Distance Mapping; monotonic (never moves backward while scrolling one direction); stable across viewport sizes. (FR-1,2,4,5; AD-4,6,7,8)
- (capability) CAP-2 Jump to any Era (click-to-launch). intent: visitor selects any Era from the rail or an in-world waypoint and the camera performs an interruptible nonlinear launch tween to it. success: travel duration scales with sequence distance within a bounded range; tween is interruptible without snapping; on arrival Motion Mode returns to free-scroll and the target's Detail Panel is available. (FR-3,19; AD-7,18)
- (capability) CAP-3 Era scenes & worldbuilding. intent: each Era renders as >=1 themed Scene Chunk in its Altitude Band, lazy-loaded by proximity, with continuous atmospheric band transitions and bounded loading/failure states. success: each Era has exactly one band + >=1 scene; resident GPU texture memory stays within the mobile budget across the full traversal; no material-recompile hitch at transitions; a jumped-to/deep-linked Era never blanks the canvas. (FR-6,7,8,9,33; AD-3,13,14)
- (capability) CAP-4 Content Truth Layer. intent: all readable/linkable/indexable content (Era summaries, Detail Panels, resume, outbound/contact/repo links) lives in the SSR'd DOM above a fixed canvas, not trapped in WebGL. success: all primary text is present in server-rendered HTML (view-source) before hydration; resume renders from one MDX source to HTML + PDF; contact + repo link reachable within one action from persistent UI in both World and Static Timeline. (FR-10,11,12,13,14; AD-2,17)
- (capability) CAP-5 Career Data Layer as single source of truth. intent: one typed structured data layer defines all Eras/Waypoints/Tech Nodes/artifacts/dates/links and feeds every surface; an offline tool-agnostic Knowledge Graph may later author/enrich it (post-MVP). success: editing an Era in the data updates world+rail+panel+route+Static Timeline with no per-surface duplication; no career fact hardcoded in scene logic; one canonical Era keyed by slug owns band+years. (FR-15,16,17,18; AD-1,8)
- (capability) CAP-6 Navigation & wayfinding. intent: a persistent Timeline Rail (jump-to-era), a year/altitude HUD, and real shareable per-era routes let visitors orient and share without getting lost. success: rail reflects data order and highlights current Era; HUD year stays consistent with the scroll mapping and after a jump; each Era is a real crawlable URL whose deep-link cold-load PLACES (no from-ground launch) and whose Back/404 behavior is defined. (FR-19,20,21; AD-11,18)
- (capability) CAP-7 Physics & artifact interaction (post-MVP). intent: story/artifact cards behave as soft-physics bodies that drift/orbit when idle and can be grabbed/inspected on demand. success: idle motion is bounded and pauses under reduced-motion / low tier; per-scene physics body count never exceeds the per-tier cap; inspecting enters inspect Motion Mode and returns cleanly. (FR-22,23,24; AD-14 physics sub-rule)
- (capability) CAP-8 Accessibility & fallback parity. intent: the product is fully usable and truthful via keyboard, reduced-motion, mobile, and no-WebGL paths, delivering full content parity. success: reduced-motion / Tier 0 / no-WebGL2 / context-lost deterministically serve the same SSR'd Static Timeline (every Era/panel/resume/link present); every Era is a focusable button (never raycast-only); flash-safety passes PEAT; focus ring >=3:1 on worst-case backdrop; era/year announced to AT. (FR-25,26,27,28; AD-9,10,16,17)
- (capability) CAP-9 Source-as-Exhibit. intent: the public repository is a first-class portfolio artifact — AI-built code that demonstrably meets Jarad's engineering standards, is AI-aware, and holds up to hostile inspection. success: CONVENTIONS.md + CI import-boundary/lint/doc checks green on main; gitleaks secret-scan publish gate with clean history; AGENTS.md/CLAUDE.md pass a cold-start locatability test; >=1 CI-guarded source-resident easter egg; a published build case study. (FR-29,30,31,32,35; AD-15)
- (capability) CAP-10 Instrumentation & consent. intent: the site captures the minimal privacy-respecting events behind its success metrics via one typed event schema, honoring privacy signals, without a consent surface that collides with onboarding. success: scroll-traversal arrival vs jump arrival are distinct named events (SM-2 computable); resume download + outbound/repo clicks + shares/revisits captured; DNT/GPC honored; consent UI keyboard+reduced-motion-safe and sequenced off the 3-5s onboarding cue. (FR-34; AD-19)
- (constraint) DOM is the truth, WebGL is a next/dynamic ssr:false island on top; LCP element is SSR text, never the canvas. Rules out spectacle-first builds and any content trapped in WebGL. (AD-2)
- (constraint) Single-authority runtime: exactly one persistent GlobalCanvas (switch scenes by visibility, never remount), one scroll authority (GSAP ScrollTrigger on Lenis, no drei ScrollControls), one RAF loop, one scrubbable camera timeline, one navigation/history authority, one panel/focus controller, one analytics event schema, one runtime quality monitor. Rules out per-section canvases, competing RAF loops, ad-hoc history/panel/event code. (AD-3,4,7,13,17,18,19)
- (constraint) State boundary: never setState in useFrame/scroll callbacks; per-frame via refs/store.getState(); React re-render only for discrete UI state. Bends every scene's implementation. (AD-5)
- (constraint) World axis is the monotonic Career Sequence, not raw calendar year; year is a derived label; Primary Track single-valued; one canonical Era keyed by slug owns band+years. Rules out year->distance layout and duplicate era records. (AD-1,8)
- (constraint) Accessibility is a full launch gate, not a floor: reduced-motion + Fidelity Tier orthogonal; deterministic degrade to one Static Timeline serving four jobs (SEO/noscript/reduced-motion/fallback); keyboard/never-raycast-only; PEAT flash-safety even in full motion; WCAG 2.2 AA. Bends scene, nav, and CI design. (AD-9,10,16,17)
- (constraint) Performance budget is a checked-in perf-budget.json enforced by CI (draw calls, triangles, resident texture memory, WebGL bundle weight, CWV); runtime auto-downgrade one tier if avg FPS<40 for >2s. Spectacle must never regress budgets. (AD-13)
- (constraint) Hosting = Cloudflare OpenNext-on-Workers + R2 (zero-egress 3D assets behind assets.jaradd.com) + KV; R3F/Three are client-only (never in the server Worker bundle <=10MiB); build on Next.js 16; next-on-pages not used; domain jaradd.com. Full pinned stack in the architecture spine companion. (AD-12; spine Stack)
- (constraint) Ways of working: truth-layer-first walking skeleton; demo-first (every increment runnable+watchable); spikes disposable EXCEPT the non-disposable foundations (single canvas/scroll/RAF, SSR Content Truth Layer/Static Timeline, Fidelity-Tier detection, perf-budget CI); experience is a first-class acceptance test. Downstream epics must be demoable-per-epic and carry experiential acceptance criteria. (PRD §16)
- (constraint) Source-available 'look, don't reuse' license (assumption, confirm flavor) — public for inspection, not an OSS/reuse product. (AD-15; PRD §5)
- (decision) Non-goals (from PRD §5): not a game (game-feel yes, win/lose/score/levels no); not built on a game engine or 2D-only renderer; not text-trapped-in-WebGL; not a bleeding-edge baseline (WebGL2-first, WebGPU optional only); not a CMS/blog platform; not a reusable portfolio-builder for others in v1; not an on-page live knowledge-graph visualization; not an open-source/reuse product (public for inspection only).
- (decision) Success signal: the site is cited/remembered in >=N recruiter/founder conversations in Q1 post-launch (SM-1, non-gated qualitative proxy); >=40% of engaged sessions that pass the first Era reach the orbit Era, scroll-traversal depth reported distinctly from jump arrivals (SM-2, primary/testable); resume downloads + outbound/repo clicks per engaged session (SM-3); shares + direct-link revisits (SM-4); repo visits/stars/clones + qualitative 'I looked at the code and...' reactions (SM-5). Counter-metrics: never regress §10 load/perf budgets, content findable without spectacle, full a11y parity.
- (decision) Companions: ADOPTED (downstream MUST read, bmad-spec does not edit) = ../../planning-artifacts/prds/prd-project-delorenj-2026-07-15/prd.md (FR-1..35 with testable consequences, glossary, NFRs, MVP scope) and ../../planning-artifacts/architecture/architecture-project-delorenj-2026-07-16/ARCHITECTURE-SPINE.md (19 ADs, stack, structural seed, diagrams). SPEC-authored = capability-traceability.md (CAP-N <-> FR <-> AD <-> MVP/post-MVP bridge). PRD addendum stays discoverable via the PRD. No content duplicated into SPEC.md.
- (assumption) Camera authoring = code-authored GSAP timeline is the runtime authority; Theatre.js at most a dev-only aid baked to state.json (AD-7). Reason: @theatre/r3f ~2yr stale.
- (assumption) Scroll-rig = hand-roll minimal single-canvas/tunnel with drei primitives; @14islands/r3f-scroll-rig behind a spike gate. Reason: 8.15.0 stale, React-19 compat unconfirmed (AD-4 topology fixed).
- (assumption) WebGL2-only at launch; WebGPU deferred (PRD §5, spine Deferred).
- (assumption) Audio muted-by-default, user-initiated, out of MVP (PRD §8 Q11).
- (assumption) Fidelity-tiering: full 3D for the two signature bookends (ground BAE, orbit->deep-space AI); lighter kinetic/particle/wireframe for transitional eras (PRD §8 Q12, §6.1).
- (assumption) MVP = 4 waypoints (BAE/Warby/Justworks/ACD) + deep-space Axioms bookend, 4 bands; physics + visual relationship lines + KG ingestion post-MVP; a genuine lower-fidelity mobile spectacle required before public launch (PRD §6).
- (assumption) Timeline facts authored into the Career Data Layer from LinkedIn (auth-gated): education=Stevens Institute of Technology, current=IntelliForia, 25+ yrs Staff Eng/Systems Architect; exact companies/dates are a data-authoring task, not a blocker (PRD §8 Q1).
- (question) SM target numbers: set N for SM-1 (conversations) and confirm the SM-2 >=40%-reach-orbit threshold (PRD §7/§8 Q9).
- (question) Source-available license flavor for the public repo (e.g. PolyForm Noncommercial, BUSL, or custom view-only) — confirm (AD-15, PRD §4.8).
- (question) Confirm the 5 architecture Fast-path calls stand: camera authoring (code GSAP), scroll-rig (hand-roll), WebGPU (deferred), audio (out of MVP), per-era fidelity tiers (AD-7/AD-4 + PRD §8 Q12-16).
- (question) MVP inclusion of a lightweight physics idle-drift teaser (FR-22-24) and visual relationship lines (FR-17) vs data-only — PRD §8 Q5/Q6 left for demo-first resolution.
- (question) Analytics vendor + production observability/RUM choice (privacy-first; field-CWV is a §10 launch gate) — event schema fixed by AD-19, mechanism open (PRD §8 Q10).
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6,8): PASS. All 10 capabilities carry intent+success; intents are WHAT (mechanisms live in ADs/spine); 9 constraints each rule something out; 8 non-goals; success signal testable (SM-2 >=40% reach orbit + counter-metrics); CAP-1..10 stable/unique; prose lean.
- (event) Self-validate Pass 2 (Preservation): PASS. All load-bearing source claims preserved — 35 FRs mapped in capability-traceability.md + retained in PRD companion; 19 ADs in spine companion + summarized in Constraints; glossary/NFRs/§16 in PRD+spine companions; SMs in Success signal; §5 non-goals lifted; 5 architecture assumptions + PRD open items surfaced. Wrapper-only drops: none.
- (event) spec finalized. SPEC.md + capability-traceability.md derived; PRD + ARCHITECTURE-SPINE.md adopted as companions (CAP IDs stable for downstream citation). Story Breakdown deliberately skipped — phase 2 (bmad-create-epics-and-stories) owns epics/stories.
