---
scope: Whole 'Career Ascent' portfolio product — the co-equal Spectacle + Content Truth layers, the Career Data Layer SSOT, single-canvas/scroll/RAF runtime, Cloudflare hosting, and the Source-as-Exhibit repo gates. Altitude: feature (keeps the epics level coherent).
purpose: build-substrate
altitude: feature
updated: 2026-07-16T03:57
---

- (direction) Intent=Create. Mode=Fast path (user chose 'draft it all now' + 'spine + interactive artifact'). Ratify-heavy run: PRD addendum already pre-decides stack/hosting/paradigm as research-grounded proposals 'for Architecture to ratify' -> those become [ADOPTED] ADs; elicitation budget spent inferring the ~6 open forks (PRD §8 Q11/Q13/Q15/Q16 + license) with [ASSUMPTION] tags. Inputs: prd.md, addendum.md, research-digest.md (all Jul 15).
- (version) Verified current on web 2026-07-16: Next.js 16.2.x LTS (App Router) | @opennextjs/cloudflare 1.20.x (full Next 16 support; next-on-pages deprecated) | React 19.x | @react-three/fiber 9.6.x | three 0.185.x | @react-three/drei 10.7.x | @react-three/rapier 2.2.x (R3F v9/React 19) | gsap 3.13.x (100% free incl. ScrollTrigger since Apr 2025) | lenis 1.3.x (use lenis/react) | zustand 5.x | @pmndrs/detect-gpu (boot tiering).
- (version) Staleness flags (web 2026-07-16): @theatre/r3f 0.7.2 last published ~2y ago, dev slowed -> maintenance risk for solo builder. @14islands/r3f-scroll-rig 8.15.0 ~1y+ stale, React 19/zustand v5 compat UNCONFIRMED vs our R3F v9 baseline -> buy-vs-build is a real risk.
- (decision) Paradigm = layered progressive-enhancement over a single-source-of-truth data core, with a single-authority runtime. One Career Data Layer (Model) projects to many surfaces (SSR DOM truth layer, WebGL spectacle, Static Timeline, per-era routes, JSON-LD); DOM projection is the truth, WebGL is a leaf enhancement island. Binds all. Prevents surfaces inventing their own facts or the spectacle becoming load-bearing for content.
- (decision) AD: SSOT Career Data Layer (FR-15) [ADOPTED]. Every surface reads the one typed Career Data Layer; no career fact hardcoded in scene logic. Prevents rail/panel/route/SEO drift.
- (decision) AD: DOM-is-truth, SSR-first [ADOPTED]. All primary content in server-rendered HTML before hydration; WebGL is a next/dynamic ssr:false 'use client' island mounted on already-SSR'd DOM; LCP element is SSR hero/timeline text, never the canvas. Prevents text-trapped-in-WebGL / SEO+a11y collapse.
- (decision) AD: Single GlobalCanvas [ADOPTED]. Exactly ONE persistent canvas fixed behind the scrolling document for the whole site; switch era scene-graphs by 'visible' prop, never mount/unmount (browsers cap WebGL contexts ~8-16). Prevents context exhaustion + material-recompile hitches at transitions.
- (decision) AD: Single scroll authority + single RAF loop [ADOPTED]. GSAP ScrollTrigger on the real (Lenis) document is the ONLY scroll authority (no drei ScrollControls in-route); Lenis autoRaf:false driven from gsap.ticker; one RAF; frameloop='demand' + invalidate() during scroll/tween/physics. Prevents DOM<->WebGL jitter + SSR-breaking overlay scroll.
- (decision) AD: State boundary [ADOPTED]. Per-frame values (camera, scrollProgress, physics) live in refs or read via store.getState() inside useFrame; NEVER setState in useFrame/scroll callbacks. React re-render reserved for discrete UI state only (activeEra, qualityTier, reducedMotion). The #1 R3F perf failure; prevents independently-built scenes each routing 60fps through React's scheduler.
- (decision) AD: One shared Sequence config drives BOTH DOM section heights AND world waypoint offsets [ADOPTED]. Sequence-to-Distance Mapping (addendum §C) is the single origin for both; derive, never hand-sync. Prevents DOM<->world drift and §10 CLS.
- (decision) AD: One scrubbable camera timeline. Author the whole ground->orbit->deep-space journey as ONE timeline; each frame maps scrollProgress->timeline position; click-to-launch tweens the SAME playhead (never hijacks scroll). Runtime authority = code-authored GSAP timeline (resolves §8 Q13). Theatre.js optional DEV-ONLY authoring, NODE_ENV-guarded, baked to a checked-in state.json — NOT a runtime dep [ASSUMPTION: given @theatre/r3f staleness, recommend code-authored; confirm].
- (decision) AD: Career Sequence axis, not raw year [ADOPTED] (FR-1/5/6). World axis is the monotonic Career Sequence; year is a derived HUD label; Primary Track is single-valued at every position; concurrent roles co-locate as artifacts. Prevents overlapping-tenure ambiguity.
- (decision) AD: Reduced-Motion & Fidelity Tier are orthogonal first-class modes [ADOPTED]. Spectacle OFF by default in code, enabled only when prefers-reduced-motion does not match; a single motion flag gates ALL camera/parallax/physics, re-evaluated on matchMedia change. Fidelity Tier 0-3 from detect-gpu at boot + PerformanceMonitor runtime with hysteresis; Tier 0 / no-WebGL2 / webglcontextlost -> Static Timeline. Prevents a11y regressions and thermal-throttle collapse.
- (decision) AD: Static Timeline = ONE component, four jobs [ADOPTED]. The same Static Timeline artifact IS the SEO body, the <noscript> mirror, the reduced-motion mode, and the low-end/context-lost fallback. Prevents four divergent fallback implementations.
- (decision) AD: Routing authority [ADOPTED] (FR-21). Real History-API per-era routes (/journey/[era]), no hash routing; each SSR title/meta/canonical/JSON-LD + OG/Twitter. Deep-link cold-load PLACES the camera (no FR-3 launch tween) and opens the panel; replaceState tracks scroll, pushState only on explicit selection; unknown/renamed slug -> graceful 404 into Static Timeline; published slugs stay resolvable via alias/redirect policy (site pivots hard, §16).
- (decision) AD: Cloudflare client/server split [ADOPTED] (addendum §N). R3F/Three live in client components only -> compile to static JS assets, never into the server Worker bundle (<=~10MiB gzip). Large 3D binaries (KTX2/Draco/Meshopt .glb/HDRI) in R2 behind a custom asset domain (assets.jaradd.com), content-hashed, Cache-Control immutable, single-range 206; R2 zero-egress is the cost win. ISR cache on KV+R2. Prevents Worker bundle-limit build failures.
- (decision) AD: Perf-budget-as-CI-gate [ADOPTED] (§10). Checked-in perf-budget JSON is the single origin of budgets; CI fails on regression (draw calls <=50 mobile/100 desktop, tris <=500k, texture mem <=256-384MB mobile, first WebGL load <6MB); runtime frame monitor auto-downgrades one tier if avg FPS<40 for >2s. Prevents spectacle silently eating the budget across independently-built scenes.
- (decision) AD: Scene contract [ADOPTED] (FR-6/7/8/33). Every Era -> >=1 Scene Chunk, data/config-driven, built from SHARED composable rendering modules (materials/particles/transitions) not N bespoke pipelines; nodes stay mounted, GPU textures/geometry disposed/reloaded outside the adjacent-era window; each scene has bounded loading + degraded per-scene fallback (isolated, never the coarse context-lost path); set pieces must match the Era's Tech Nodes (no anachronisms). Sub-rule: physics (post-MVP) scoped to active-era props, paused off-screen, disabled Tier<=1/reduced-motion, per-tier body caps. Prevents unmaintainable per-era divergence + memory growth.
- (decision) AD: Source-as-Exhibit gates (FR-29/30/31/32/35). CONVENTIONS.md + CI import-boundary/module-graph rules; gitleaks secret-scan as a hard publish gate (history verified clean); AGENTS.md/CLAUDE.md + per-module docs (cold-start locatability test); >=1 source-resident easter egg guarded by a CI test; source-available 'look, don't reuse' LICENSE [ASSUMPTION: license flavor e.g. PolyForm Noncommercial / BUSL / custom view-only — confirm].
- (assumption) Open forks resolved by inference (Fast path, confirm in review): Q4 MVP waypoints = BAE/Warby/Justworks/ACD + deep-space Axioms bookend (ratifies §6). Q5 Relationship Lines = data + panel notes in MVP, visual lines post-MVP. Q6 Physics = post-MVP (Phase 3), optional lightweight idle-drift teaser. Q11 Audio = muted-by-default, user-initiated, out of MVP. Q12 Fidelity = full 3D for signature bookends (ground BAE + orbit/deep-space AI), lighter kinetic/particle/wireframe for transitional eras. Q15 scroll-rig = hand-roll minimal single-canvas/tunnel with drei primitives (lean), r3f-scroll-rig behind a spike gate due to staleness. Q16 = WebGL2-only at launch, WebGPU deferred.
- (event) Fast-path spine drafted from memlog: 15 ADs (13 [ADOPTED] ratified from PRD/addendum, 2 carrying [ASSUMPTION] tags), paradigm, conventions, stack, structural seed + deployment, capability map, deferred. Next: reviewer gate + interactive artifact.
- (event) Reviewer gate ran (4 parallel: reconcile, rubric, versions, adversarial). Verdicts: versions PASS-WITH-CONCERNS (all pins current, both staleness flags CONFIRMED); rubric PASS-WITH-CONCERNS; reconcile + adversarial NEEDS-WORK. Reviews saved under reviews/.
- (decision) Gate fixes applied (Finalize, self-applied): tightened AD-1 (one canonical Era record owns band+years, others reference by slug — kills dual-entity divergence); AD-9 (deterministic surface selection: reduced-motion/Tier0/no-WebGL2/context-lost => no canvas, Static Timeline; runtime monitor delegated to AD-13); AD-13 (owns single runtime quality monitor + resident-window budget assertion); AD-14 (adjacent-era window defined once in order units, single residency authority, pinned failure taxonomy); AD-11 (history via AD-18).
- (decision) New ADs from gate: AD-16 Accessibility launch invariant (FR-27 + §10 a11y cluster: keyboard/never-raycast-only, PEAT flash-safety even in full-motion, focus-ring dual-backdrop contrast, persistent text >=4.5:1, SR live region, target-size/reflow/forced-colors, axe+PEAT CI gate). AD-17 one Detail-Panel & focus controller (single activePanel slice, one open, focus restore). AD-18 single navigation/history authority (reconciles AD-4<->AD-11<->Next router; one writer, route->placement one-way). AD-19 single analytics event schema (typed registry, slug-keyed, scroll-arrival vs jump-arrival distinct => SM-2 computable).
- (decision) Conventions tightened: canonical Era identity id===slug; order/band read from the one Era record; positionHint in world units rel. to segment origin, world-Y owned by sequence.config; TechNode->Era membership by explicit assignment not year overlap; one-writer-per-store-slice; single viewport basis for AD-6. Capability map corrected (a11y was falsely mapped to AD-2/9/13). Stack: softened Next 'LTS' label; noted lockfile-pin + spike for detect-gpu/postprocessing. Deferred: added production observability/RUM + error tracking + rollback (schema fixed by AD-19, mechanism deferred).
- (event) Spine now 19 ADs (17 [ADOPTED], AD-7 + AD-15 carry [ASSUMPTION] tags for Theatre-optional + license flavor). Lint clean. Next: interactive artifact + close.
- (event) spine finalized. status:final. Interactive artifact 'Career Ascent — Architecture Console' published (paradigm + interactive dependency diagram + 19 filterable ADs + stack + deferred + reviewer-gate systems check). Deliverables: ARCHITECTURE-SPINE.md, architecture-deck.html, reviews/*.md, .memlog.md. External handoffs: none configured. Next recommended: bmad-spec (adopt spine as spec companion) then bmad-create-epics-and-stories.
